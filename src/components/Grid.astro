---
import Item from './GridItem.astro';

const { items, content } = Astro.props;
---

<div class="grid gap-6 row-gap-5 md:grid-cols-2 lg:grid-cols-4 -mb-6">
  {items.map((item) => <Item item={item} content={content} />)}
</div>
